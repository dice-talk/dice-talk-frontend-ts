import { View, Text, StyleSheet } from "react-native";

const LoveArrowMatch = () => {
  return (
    <View style={styles.container}>
      <Text>LoveArrowMatch</Text>
    </View>
  );
};

export default LoveArrowMatch;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
});